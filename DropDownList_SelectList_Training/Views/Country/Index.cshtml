@using EntityLayer.Concrete
@model List<Country>

@{
    Layout = "_Layout";
}

<h1>Ülke Listesi</h1>
<br />
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th scope="col">Ülke</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>
                    <a href="/Country/DeleteCountry/@item.Id" onclick="return confirm('Dosyayı silmek istediğinize emin misiniz?');" class="btn btn-danger">Sil</a>
                    <a href="/Country/UpdateCountry/@item.Id" class="btn btn-success">Düzenle</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<a href="/Country/AddCountry" class="btn btn-info mt-3">Ekle</a>